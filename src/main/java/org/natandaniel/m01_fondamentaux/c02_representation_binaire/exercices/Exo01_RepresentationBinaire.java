package org.natandaniel.m01_fondamentaux.c02_representation_binaire.exercices;

/**
 * Exercices — Représentation binaire.
 *
 * À implémenter avec de l'arithmétique pure (les opérateurs de bits viennent plus tard,
 * en c04_operateurs). Vérifie-toi avec {@code solutions/Exo01_RepresentationBinaire}
 * et le test {@code Exo01_RepresentationBinaireTest}.
 */
class Exo01_RepresentationBinaire {

    /**
     * Convertit un tableau de 8 bits (bits[0] = bit de poids fort) en sa valeur
     * non signée 0..255. Exemple : {1,1,1,1,1,1,1,1} → 255 ; {0,0,0,0,1,0,1,0} → 10.
     * Indice : méthode de Horner — pour chaque bit, valeur = valeur * 2 + bit.
     */
    static int valeurNonSignee(int[] bits) {
        // TODO
        throw new UnsupportedOperationException("À implémenter");
    }
}
