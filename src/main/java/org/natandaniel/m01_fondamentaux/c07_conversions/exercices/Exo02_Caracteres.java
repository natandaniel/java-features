package org.natandaniel.m01_fondamentaux.c07_conversions.exercices;

/**
 * Exercices — char comme entier.
 *
 * Implémente chaque méthode en exploitant l'arithmétique sur les char (un char est
 * un entier 16 bits non signé). Vérifie-toi avec {@code solutions/Exo02_Caracteres}
 * et le test {@code Exo02_CaracteresTest}.
 */
class Exo02_Caracteres {

    /** Renvoie la distance alphabétique de a à b (ex. 'A' → 'C' = 2 ; peut être négative). */
    static int distanceAlphabetique(char a, char b) {
        // TODO
        throw new UnsupportedOperationException("À implémenter");
    }

    /**
     * Convertit une lettre minuscule 'a'..'z' en majuscule via l'arithmétique sur char
     * (sans Character.toUpperCase). Tout autre caractère est renvoyé tel quel.
     * Indice : l'écart entre 'a' et 'A' est constant.
     */
    static char enMajuscule(char c) {
        // TODO
        throw new UnsupportedOperationException("À implémenter");
    }
}
