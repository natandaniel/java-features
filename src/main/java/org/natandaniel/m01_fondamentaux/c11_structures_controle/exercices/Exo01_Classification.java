package org.natandaniel.m01_fondamentaux.c11_structures_controle.exercices;

/**
 * Exercices — if / else if / else.
 */
class Exo01_Classification {

    /**
     * Classe une température en "gel" (&lt; 0), "frais" (&lt; 15), "tempere" (&lt; 25),
     * "chaud" (&lt; 35) ou "canicule" (le reste). Les bornes hautes sont exclusives.
     */
    static String classerTemperature(double celsius) {
        throw new UnsupportedOperationException("À implémenter");
    }

    /** Renvoie -1 si a &lt; b, 0 si a == b, 1 si a &gt; b — sans utiliser Integer.compare. */
    static int comparer(int a, int b) {
        throw new UnsupportedOperationException("À implémenter");
    }
}
